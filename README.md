# Clear deposit Project

This project contains a contract for clear deposit.
The following commands are used while development and deployment:

```shell
yarn hardhat compile
yarn hardhat test
yarn hardhat deploy --network goerli
```